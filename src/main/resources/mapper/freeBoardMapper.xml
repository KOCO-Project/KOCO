<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="freeBoardMapper">

	<select id="getFreeBoard" resultType="freeBoard">
		SELECT * FROM KOCO_BOARD WHERE BOARD_NO = #{boardNo}
	</select>

	<select id="freeBoardList" resultType="freeBoard">
		SELECT * FROM KOCO_BOARD WHERE BOARD_TITLE LIKE '%' || #{searchKeyword} || '%'
		ORDER BY BOARD_NO DESC
	</select>

	<insert id="boardRegister">
		INSERT INTO KOCO_BOARD (BOARD_TITLE, BOARD_CONTENT)
		VALUES(#{boardTitle}, #{boardContent})
	</insert>

	<update id="boardUpdate">
		UPDATE KOCO_BOARD SET BOARD_TITLE = #{boardTitle}, BOARD_CONTENT = #{boardContent} WHERE BOARD_NO = #{boardNo}
	</update>

	<delete id="boardDelete">
		DELETE FROM KOCO_BOARD WHERE BOARD_NO = #{boardNo}
	</delete>

</mapper>
